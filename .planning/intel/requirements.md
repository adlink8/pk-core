# Migrated Requirements

Canonical requirement IDs and phase mappings are recorded in `.planning/REQUIREMENTS.md`. Historical capabilities are marked complete from legacy STATE; Phase 14 production backfill, canonicalization, retrieval canary and lifecycle remain active.
