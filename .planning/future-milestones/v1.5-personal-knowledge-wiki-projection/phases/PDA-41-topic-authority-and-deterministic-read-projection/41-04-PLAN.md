---
phase: 41
plan: 04
type: execute
wave: 4
depends_on: ["41-00", "41-01", "41-02", "41-03"]
files_modified:
  - tests/contract/test_topic_projection.py
  - tests/contract/test_api_server.py
  - .planning/future-milestones/v1.5-personal-knowledge-wiki-projection/phases/PDA-41-topic-authority-and-deterministic-read-projection/41-VERIFICATION.md
autonomous: true
requirements: [WIKI-01]
milestone_status: preplanned_not_active
execution_authorization: none
must_haves:
  truths:
    - "Phase 41 的 read projection 被证明可重建、无新事实权威、无写副作用，并在 privacy/partial/offline/error 情况下诚实降级。"
    - "所有 API/contract 测试都以 audited v1.4 接口为准；无法满足的前提记录为 blocker，不能为了通过而放松安全或身份边界。"
  artifacts:
    - path: "tests/contract/test_topic_projection.py"
      provides: "Authority/freshness/evidence/privacy/zero-side-effect end-to-end read contract"
    - path: ".planning/future-milestones/v1.5-personal-knowledge-wiki-projection/phases/PDA-41-topic-authority-and-deterministic-read-projection/41-VERIFICATION.md"
      provides: "Phase 41 execution-time verification record template and phase-boundary proof"
  key_links:
    - from: "Phase 41 verification"
      to: "v1.4 milestone audit"
      via: "required revalidation of accepted projection/evidence/transport contracts"
      pattern: "v1\.4|revalidation|side.effect"
---

<objective>
将 Phase 41 的关键不变量收束为可重复的契约测试和执行期验证记录：它必须是由上游 authority 可重建的只读投影，而不是暗中增长的新知识库或缓存权威。

当前只创建未来验证计划；不运行生产数据库操作、不宣称已验收，也不把 v1.5 写入根 STATE/ROADMAP。
</objective>

<threat_model>
- 局部单元测试可能遗漏 transport、privacy、partial failure 与 side effect；需要跨层读取契约证明。
- v1.4 尚未完成，接口可能变化；如果不设明确重验证门，预计划会对不存在的实现产生虚假保证。
- “无写入”若只靠设计声明，很容易被 helper 或 logging/import side effect 破坏；测试需在每次 read 前后对关键 authority 状态做对照。
</threat_model>

<tasks>
<task type="execute">
  <name>Task 1: 补齐 Phase 41 端到端只读与可重建反证</name>
  <files>tests/contract/test_topic_projection.py; tests/contract/test_api_server.py</files>
  <action>
增加跨层 fixture tests：从 canonical key → direct service → REST 的 Topic get/list/backlinks 逐一对照，忽略生成时间但严格比较 schema、canonical identity、snapshot binding、authority/evidence refs、freshness、partial 与 checksum。验证相同 authority fixture 重建得到相同投影 checksum；删除任何内存结果后再次读取仍可从 authority 重建。

对每次读取记录 authority DB row counts/checksums、Serving Snapshot、watermark、Active Pointer、mock Chroma/Provider/external-action calls，并断言读取结束后完全不变。注入 privacy seal、authority offline、snapshot mismatch、evidence mismatch、bad key 和 empty relationship；断言安全 code、局部降级和零异常泄露。
  </action>
  <verify>运行聚焦 pytest；用 mock 或 fixture 强制 Provider/Chroma/write transaction/external action 在被调用时失败，证明正常路径没有触及它们。</verify>
  <done>Phase 41 具有正向正确性和负向零副作用证据，而非仅由文档声称 read-only。</done>
</task>

<task type="execute">
  <name>Task 2: 创建执行时验证清单并封存 v1.4 依赖门</name>
  <files>.planning/future-milestones/v1.5-personal-knowledge-wiki-projection/phases/PDA-41-topic-authority-and-deterministic-read-projection/41-VERIFICATION.md</files>
  <action>
把未来验证分成：v1.4 prerequisite revalidation、parser/service/REST 自动测试、privacy/partial/offline 负向测试、side-effect fingerprint、人工 UAT（长中文/长 ID/metadata-only）和 Phase 42 handoff。清楚记录成功证据、失败含义、允许/禁止的降级和必须停止的 blocker。

验证文档必须标记为 `preplanned_not_active`/`planned`，绝不记录 passed 或伪造执行结果；在 v1.4 完成后由实际执行证据填充。明确本阶段不验证 Topic Page UI、materialized page metadata、stale rebuild、LLM narrative 或编辑器。
  </action>
  <verify>审查 verification checklist 是否覆盖 WIKI-01 的 identity、authority binding、truth separation、evidence、partial、safe recovery、zero write 及 v1.4 安全 prerequisite。</verify>
  <done>任何实施者都能根据清单判断 Phase 41 是否真正完成，且无法把尚未完成的 v1.4/v1.5 误标为通过。</done>
</task>
</tasks>

<verification_criteria>
- 运行时数据可从 fixtures/upstream authority 重建；没有 page body、事实或 semantic link 被持久化为 SSOT。
- 读路径在 normal、privacy sealed、partial、offline、snapshot mismatch 和 bad key 下都有可审计的 expected result。
- 指纹/spy 测试证明数据库写、Provider、Chroma/KU 写、external action、promotion/active-pointer 更新均为零。
- 执行期 validation 文件不把本预规划状态写成 `passed`，并把 v1.4 audit/security revalidation 设为硬前提。
</verification_criteria>

<revalidation_checkpoint>
Phase 41 开始前和结束后均需读取真实 v1.4 audit、interface snapshot 与 UAT 结果。若它们与此预计划的 assumptions 冲突，暂停并更新 v1.5 research/plan/checker，而不是以适配测试方式绕过安全或 SSOT 不变量。
</revalidation_checkpoint>
