---
phase: 42
plan: 02
type: execute
wave: 2
depends_on: [42-01]
status: preplanned_not_active
activation_precondition: "42-01 complete plus verified Phase 41 topic and evidence read contracts"
files_modified:
  - apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx
  - apps/personal_decision_cockpit/src/components/knowledge/EvidenceDrawer.tsx
  - apps/personal_decision_cockpit/src/components/knowledge/TopicBacklinks.tsx
  - apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx
  - apps/personal_decision_cockpit/src/test/TopicPage.test.tsx
  - apps/personal_decision_cockpit/src/test/EvidenceDrawer.test.tsx
  - apps/personal_decision_cockpit/src/test/fixtures/wiki-topic-page.json
  - apps/personal_decision_cockpit/src/test/fixtures/wiki-topic-partial.json
autonomous: true
requirements: [WIKI-02]
must_haves:
  truths:
    - "Project、Goal、Decision 的 Topic Page 是同一份 server projection 的三种 typed read rendering；它们不推断事实、不把 External 变成 Personal、不把 Recommendation/Forecast/Inference 显示为 Fact。"
    - "每个可见结论都保留 authority、snapshot/checksum、freshness、limitations 与 server-issued evidence reference；sealed、missing、mismatch、partial 和 empty 都有不同且准确的界面。"
    - "Backlinks 只显示 Phase 41 明确提供的 typed joins 与 `join_basis`，可诚实为空；绝不触发向量相似度、LLM 关系猜测或客户端 authority join。"
    - "Wiki 页面只提供向 Cockpit State/Decision/Action 页面和 `/evidence` 的导航链接；不包含 prepare、preview、confirm、action/outcome 记录、suppress、provider 或任何 POST 写入路径。"
  artifacts:
    - path: apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx
      provides: P0 Project/Goal/Decision 的 read-only 页面 grammar、epistemic sections 和 typed recovery
    - path: apps/personal_decision_cockpit/src/components/knowledge/EvidenceDrawer.tsx
      provides: 只接受 server-issued opaque evidence reference 的有界、privacy-aware evidence drill-down
    - path: apps/personal_decision_cockpit/src/components/knowledge/TopicBacklinks.tsx
      provides: 带 join basis、authority 与 snapshot provenance 的 explicit backlink list
  key_links:
    - from: apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx
      to: apps/personal_decision_cockpit/src/api/hooks.ts
      via: exact `topic.get` / `topic.backlinks` envelopes determine every page section and its degraded state
    - from: apps/personal_decision_cockpit/src/components/knowledge/EvidenceDrawer.tsx
      to: executed Phase 41 evidence resolver
      via: opaque evidence reference plus expected authority/snapshot/checksum; no raw text/query URL transport
    - from: apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx
      to: apps/personal_decision_cockpit/src/pages/decisions/DecisionWorkspacePage.tsx
      via: navigation-only `Link` handoff; guarded confirmation components and orchestration client never import into Wiki
---

<objective>
把 Phase 41 的 P0 topic projection 渲染为可审计、可下钻、可浏览的 Project/Goal/Decision
主题页：事实类型、历史、冲突、外部上下文、决策反馈、限制和 evidence 各自清楚可见，同时
把 Decision Workspace 的确认和 Action/Outcome 记录完整留在 Cockpit 工作流中。
</objective>

<threat_model>
- T-42-05：流畅的 Topic summary 抹平 Fact、Observation、Inference、Forecast、Recommendation 与 historical/conflict，用户把候选解释当作当前个人事实。缓解：固定 section grammar、显式标签/aria 文案和 fixture/DOM assertions。
- T-42-06：Evidence Drawer 接收 raw text、raw evidence ID、query-string values 或 client-composed links，泄露隐私或绕过 privacy sealing。缓解：仅传 server-issued opaque reference + expected binding；封存/缺失/mismatch 只显示 safe state/reason。
- T-42-07：empty backlinks/section 被 authority failure 冒充，或 vector/LLM 猜测被渲染成关系。缓解：只读取 explicit `join_basis`，区分 empty/partial/unavailable，禁止 similarity、Provider 与 client joins。
- T-42-08：Wiki 页面导入 ConfirmDrawer、NewSessionFlow、orchestration 或 POST helper，使知识页重复 Decision Workspace 并扩大写入面。缓解：仅 React Router outbound links；源文件和 DOM tests 均验证零 guarded action control。
- T-42-09：长中文、opaque ID、checksum 或 partial messages 破坏移动端键盘使用和 evidence navigation。缓解：使用现有 StatePanel/focus patterns，添加 320px/keyboard/long-content component assertions；完整真实 UAT 仍留 Phase 44。
- 阻断条件：若 stable evidence resolver 未经 v1.4/Phase41 execution 验证，Evidence Drawer 只能显示 typed unavailable state，不能退回 iframe 抓取、legacy graph 或 LLM summary。
</threat_model>

<tasks>
<task type="execute">
  <name>实现只读的 P0 Topic Page grammar 与 type-specific section adapters</name>
  <files>apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx; apps/personal_decision_cockpit/src/test/TopicPage.test.tsx; apps/personal_decision_cockpit/src/test/fixtures/wiki-topic-page.json; apps/personal_decision_cockpit/src/test/fixtures/wiki-topic-partial.json</files>
  <read_first>42-01-PLAN.md; ../PDA-41-topic-authority-and-deterministic-read-projection/41-VERIFICATION.md; apps/personal_decision_cockpit/src/pages/state/PersonalStatePage.tsx; apps/personal_decision_cockpit/src/pages/external/ExternalContextPage.tsx; apps/personal_decision_cockpit/src/pages/decisions/DecisionWorkspacePage.tsx; apps/personal_decision_cockpit/src/components/authority/AuthorityBadge.tsx; apps/personal_decision_cockpit/src/components/authority/SnapshotChip.tsx; apps/personal_decision_cockpit/src/components/feedback/FreshnessBadge.tsx; apps/personal_decision_cockpit/src/components/feedback/StatePanel.tsx; apps/personal_decision_cockpit/src/api/schemas.ts; apps/personal_decision_cockpit/src/api/hooks.ts</read_first>
  <action>实现共享 `TopicPage` frame，并按 `project|goal|decision` 使用服务端明确的 section payload adapter，而不是由前端 confidence/文案判断类型。页面固定顺序为：header（P0 type、freshness、generated time、authority/snapshot/checksum chips）、current context、Facts、Observations、Inferences、Forecasts/Recommendations、Historical/Superseded/Conflict、related Personal State、decision feedback、External Context、Backlinks、limitations/privacy notices、Evidence Drawer。每一区块使用文字+图标/aria label，不以颜色作为唯一信号；Personal 与 External 在 heading、authority chip、source/region/validity display 上永久分区。数据为空、partial、unavailable、invalid/type mismatch 或 snapshot mismatch 时复用 typed `StatePanel`/recovery 模式，不能以零计数或旧 UI cache 冒充 fresh。fixtures 只含合成 opaque IDs/安全文本，覆盖所有 epistemic/lifecycle 与 degraded 类型。</action>
  <acceptance_criteria>三条 typed routes 都能在 fixture 下渲染共有 header 与正确 typed sections；Fact、Observation、Inference、Forecast、Recommendation、Historical/Conflict、External Context 在 DOM 中有不同可访问 section/label；External content 不会出现在 Facts 或 Personal section；partial/unavailable fixture 有 limitation 与 recovery UI 并不显示“当前完整结论”；test fixture 不包含 raw messages、KU values、真实用户标识、路径或 secret。</acceptance_criteria>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/TopicPage.test.tsx src/test/wikiSchemas.test.ts; Pop-Location</verify>
</task>

<task type="execute">
  <name>增加有界 Evidence Drawer 与 explicit Backlinks，并保留 `/evidence` 的诊断兼容性</name>
  <files>apps/personal_decision_cockpit/src/components/knowledge/EvidenceDrawer.tsx; apps/personal_decision_cockpit/src/components/knowledge/TopicBacklinks.tsx; apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx; apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx; apps/personal_decision_cockpit/src/test/EvidenceDrawer.test.tsx; apps/personal_decision_cockpit/src/test/TopicPage.test.tsx</files>
  <read_first>../PDA-41-topic-authority-and-deterministic-read-projection/41-VERIFICATION.md; apps/personal_decision_cockpit/src/pages/evidence/EvidencePage.tsx; apps/personal_decision_cockpit/src/api/hooks.ts; apps/personal_decision_cockpit/src/api/client.ts; apps/personal_decision_cockpit/src/components/feedback/StatePanel.tsx; apps/personal_decision_cockpit/src/components/authority/SnapshotChip.tsx; apps/personal_decision_cockpit/src/test/TypedRecoveryPanel.test.tsx</read_first>
  <action>`EvidenceDrawer` 只接受 `topic.get` section 随附的 server-issued opaque evidence reference 和 expected authority/snapshot/checksum，不把 raw evidence、title、personal subject/value 写入 URL、query string、localStorage、console 或 test snapshot。它调用 executed Phase 41 的 read-only evidence resolver 后，只渲染 evidence type、opaque reference ID、authority、binding match、privacy/sealing status、eligibility/reason 与 safe deeper link；sealed/missing/mismatch/unavailable 必须逐项说明且不得以 LLM/legacy graph 兜底。`TopicBacklinks` 只渲染 `from_topic_id`、`to_topic_id`、`relation_type`、`join_basis`、source authority/snapshot/checksum 等 server explicit joins；empty、partial、unavailable 分别显示，绝不运行 vector similarity、semantic search 或 client-side join。`EvidencePage` 保持 `/evidence` legacy diagnostic widget 入口，并明确它不是 Topic/current-state authority；可以提供回到 Knowledge directory 的导航但不可成为 Drawer data source。</action>
  <acceptance_criteria>Drawer query/API input 不含 raw evidence text 或 personal values，且 sealed/missing/mismatch/unavailable fixture 分别显示 safe reason；empty backlink state 文案表示“没有目前可证明的反向链接”，partial/unavailable 不显示 0-count conclusion；rendered backlink 明确包含 server `join_basis` 与 source provenance；`/evidence` 仍可加载 legacy diagnostic surface，但 Topic evidence 读取不依赖 iframe/widget。</acceptance_criteria>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/EvidenceDrawer.test.tsx src/test/TopicPage.test.tsx src/test/appSmoke.test.tsx; Pop-Location</verify>
</task>

<task type="execute">
  <name>证明 Wiki 只读，并通过导航、键盘、长文本与禁止写入回归</name>
  <files>apps/personal_decision_cockpit/src/pages/knowledge/TopicPage.tsx; apps/personal_decision_cockpit/src/components/knowledge/EvidenceDrawer.tsx; apps/personal_decision_cockpit/src/components/knowledge/TopicBacklinks.tsx; apps/personal_decision_cockpit/src/test/TopicPage.test.tsx; apps/personal_decision_cockpit/src/test/EvidenceDrawer.test.tsx; apps/personal_decision_cockpit/src/test/appSmoke.test.tsx</files>
  <read_first>apps/personal_decision_cockpit/src/pages/decisions/DecisionWorkspacePage.tsx; apps/personal_decision_cockpit/src/pages/sessions/SessionPage.tsx; apps/personal_decision_cockpit/src/components/decision/ConfirmDrawer.tsx; apps/personal_decision_cockpit/src/components/decision/NewSessionFlow.tsx; apps/personal_decision_cockpit/src/api/orchestration.ts; apps/personal_decision_cockpit/src/api/proactive.ts; apps/personal_decision_cockpit/src/components/layout/MobileNav.tsx; 42-CONTEXT.md</read_first>
  <action>为 Project/Goal/Decision TopicPage 的 interaction tests 添加明确的 read-only allowlist：只允许目录/返回、打开 Evidence Drawer、关闭 Drawer、以及 `Link` 到 `/state/:domain`、`/decisions/:id`、`/actions` 或 `/evidence`（只有 Phase 41 payload 提供稳定目标时才显示）。断言 TopicPage/EvidenceDrawer/TopicBacklinks 不导入 `ConfirmDrawer`、`NewSessionFlow`、`sessionPrepare`、`sessionConfirm`、`apiPost`、`orchestration`、`proactive` 或 provider client；也不渲染“确认”“prepare”“preview”“记录行动/结果”“抑制”按钮。用长中文标题、opaque ID/checksum、empty/partial card 与 keyboard Tab/Escape Drawer case 验证 focus、close、break-all/overflow 语义；任何 action link 必须只是 navigation，不创建 mutation mock calls。</action>
  <acceptance_criteria>源级与组件级回归均证明 Wiki components 的 POST/orchestration/provider/直接 `fetch`/semantic-search 调用数为 0；Decision Topic 只可导航至既有 Decision Workspace，不能在 Topic Page 触发 session 或 confirmation；Escape 关闭 Drawer 并将焦点返回触发控件；长中文/opaque ID 在 320px CSS test or browser fixture 中不会隐藏主状态、evidence 或 limitation；所有 route/component tests 在无 live private authority 条件下通过。</acceptance_criteria>
  <verify>Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/TopicPage.test.tsx src/test/EvidenceDrawer.test.tsx src/test/KnowledgeDirectoryPage.test.tsx src/test/appSmoke.test.tsx; npm run build; Pop-Location; $wikiSources = @('apps/personal_decision_cockpit/src/pages/knowledge','apps/personal_decision_cockpit/src/components/knowledge'); $forbidden = rg -n "fetch\(|apiPost|ConfirmDrawer|NewSessionFlow|sessionPrepare|sessionConfirm|orchestration|proactive|provider|semantic|use.*Search|POST" $wikiSources; if ($LASTEXITCODE -eq 0) { Write-Error "Wiki read-only boundary violation"; exit 1 } elseif ($LASTEXITCODE -gt 1) { exit $LASTEXITCODE }</verify>
</task>
</tasks>

<verification>
Push-Location apps/personal_decision_cockpit; npm run test -- --run src/test/wikiSchemas.test.ts src/test/KnowledgeDirectoryPage.test.tsx src/test/TopicPage.test.tsx src/test/EvidenceDrawer.test.tsx src/test/appSmoke.test.tsx; npm run build; Pop-Location
</verification>

<success_criteria>
WIKI-02 的 Project、Goal、Decision 页面成为长周期上下文的只读解释层：能下钻证据并显示
backlinks、历史、冲突、external 与限制，却不会把任一候选结论升级为事实，也不会复刻或绕过
Cockpit 的 prepare → preview → confirm、Action/Outcome 或其他写入流程。
</success_criteria>
