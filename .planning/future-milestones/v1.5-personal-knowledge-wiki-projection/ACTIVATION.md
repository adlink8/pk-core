# v1.5 Activation and Lifecycle Integrity

This is a **preplanning package**. It must not be treated as a root `.planning/ROADMAP.md` update or a completed milestone.

When v1.4 is genuinely completed:

1. Run the normal GSD milestone completion/audit flow for v1.4.
2. Switch to v1.5 with a fresh explicit user authorization.
3. Promote these requirements and phase documents only after reconciling them with real v1.4 code, runtime and UAT evidence.
4. Re-run research/plan checker if v1.4 interfaces or security results changed.

The following states are invalid until then:

- marking Phase 41–44 active or complete in root `STATE.md`;
- representing the personal Wiki as an implemented product;
- calling a plan-only projection cache fresh or authoritative;
- executing any code, database schema, service, provider or index change from this package.

